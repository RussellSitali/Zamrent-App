import {View, Text} from "react-native";

export default function profilePayments(){
    return(
        <View>
            <Text style={{justifyContent:"center", alignItems:"center", fontSize:20,}}>This is the payments page</Text>
        </View>
    );
}